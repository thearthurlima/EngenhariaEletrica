programa
{
	funcao inicio()
	{
		real salario, percentualDoReajuste, salarioReajustado

		escreva("Qual o salário?: ")
		leia(salario)

		se(salario < 500)
		{
			 percentualDoReajuste = (salario * (1+0.15))
		}
		senao se(salario < 1000)
		{
			percentualDoReajuste = (salario * (1+0.1))
		}
		senao
		{
			percentualDoReajuste
		}














		escreva(salarioReajustado)
	}
}
