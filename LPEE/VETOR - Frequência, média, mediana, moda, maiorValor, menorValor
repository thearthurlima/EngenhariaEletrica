#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#define MAX 100
#define NUM_MAX 2000

/*Dado um vetor com valores digitados pelo usuário, encontre:
  a) Frequência em que cada elemento aparece no vetor;
  b) A média, a mediana (organizar o vetor) e a moda;
  c) Maior e menor valor.*/

int main ()
{
	int i, j, flag, vetor[MAX], frequencia[NUM_MAX], tamVetor;
	
	printf("Entre com o numero de elementos do vetor: ");
	scanf("%d", &tamVetor);
	
	printf("Entre com os %d valores do vetor: ", tamVetor);

	for(i = 0; i < tamVetor; i++) //Lê os elementos do vetor
		scanf("%d", &vetor[i])
		
	for(i = 0; i < tamVetor; i++) {
		flag = 0;
		for(j = 0; j < tamVetor; j++) {
			if(vetor[i] == vetor[i+j])
			
		}
	}
	
	
	
	
	
	
	
	
	
	/*for(i = 0; i < NUM_MAX; i++) //Zera todas as posições do vetor frequência
		frequencia[i] = 0;

	
	for(i = 0; i < tamVetor; i++) {
		frequencia[vetor[i]]++;
	}
	
	for(i = 0; i < NUM_MAX; i++) {
		if(frequencia[i] != 0)
		printf("\nNumero: %d  -  %d vez(es)", i, frequencia[i]);
	}*/
	
	
	//b) A média, a mediana (organizar o vetor) e a moda:
	
	/*int media;
	float somatorio = 0.0;
	
	for(int i = 0; i < tamanhoDoVetor; i++)
	{
		somatorio += vetor[i];
	}
	
	media = somatorio/tamanhoDoVetor;
	
	for(int i = 0; i < tamanhoDoVetor-1; i++)
	{
		if(vetor[i] > vetor[i+1])
		{
			auxiliar = vetor[i];
			vetor[i] = vetor[i+1];
			vetor [i+1] = auxiliar;
		}
	}*/
	
	
	
	
	
}

